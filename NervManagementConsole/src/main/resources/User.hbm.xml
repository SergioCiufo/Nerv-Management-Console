<?xml version="1.0" encoding="UTF-8"?>
<hibernate-mapping>
	<class name="com.company.nervManagementConsole.model.User"
		table="USERS">
		<!-- Identificativo univoco dell'utente -->
		<id name="idUser" column="userId">
			<generator class="identity" />
		</id>

		<!-- Informazioni di base dell'utente -->
		<property name="name" column="name" not-null="true" />
		<property name="surname" column="surname" not-null="true" />
		<property name="username" column="username" not-null="true" />
		<property name="password" column="password" not-null="true" />
		


<!-- 
<set name="members" table="MEMBERS" cascade="all">
			<key column="id_user" />
			<one-to-many class="Member" />
		</set>
 -->
		
<!-- 
<set name="simulations" table="SIMULATIONS" cascade="all">
			<key column="id_user" />
			<one-to-many class="Simulation" />
		</set>

		<set name="participants" table="SIMULATION_PARTICIPANTS"
			cascade="all">
			<key column="id_user" />
			<one-to-many class="SimulationParticipant" />
		</set>

		<set name="missions" table="MISSIONS" cascade="all">
			<key column="id_user" />
			<one-to-many class="Mission" />
		</set>

		<set name="missionParticipants" table="MISSION_PARTICIPANTS"
			cascade="all">
			<key column="id_user" />
			<one-to-many class="MissionParticipants" />
		</set>

		<set name="missionArchive" table="MISSION_ARCHIVE" cascade="all">
			<key column="id_user" />
			<one-to-many class="MissionArchive" />
		</set>
 -->
		
	</class>
</hibernate-mapping>